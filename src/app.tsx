import * as React from "react";
import * as ReactDOM from "react-dom";
import Controls from "./Controls";

const App: React.FC = () => <Controls />;

ReactDOM.render(<App />, document.getElementById("main"));
